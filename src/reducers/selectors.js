export const searchArtistSelector = state => {
  return state.selected.searchArtist;
};
export const selectedArtistSelector = state => {
  return state.selected.artist;
};
export const selectedAlbumSelector = state => {
  return state.selected.album;
};
